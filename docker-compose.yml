services:
  elden-ring-item-tracker-web:
    build: .
    container_name: elden-ring-item-tracker-web
    restart: unless-stopped
    ports:
      - "5000:5000"
    networks:
      local-web:
        ipv4_address: 172.20.0.11

networks:
  local-web:
    external: true